# ch27 + ch28 深入篇 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 新增 Part 8「深入篇·模型与运行时内幕」两章（ch27 模型/凭证/路由、ch28 运行时韧性），各 3 个 🔬 深度块 + 7–10 张机制 SVG，外加 ch6 一张缓存补充图。

**Architecture:** 内容写在新文件 `src/part9.py`（`LESSON_27` / `LESSON_28`），沿用全书生成器架构（`shell.PAGES` + `registry.CONTENT` 同步，`build.py` 渲染）。每章 = lead + 🔌analogy + 🌍macro + 3 深度块（机制说明 + verbatim 源码 codefile + 机制 SVG）+ 🎯设计框 + 📌要点卡 + quiz。

**Tech Stack:** 纯 Python 3 静态站点生成器（零依赖）；HTML + 内联 SVG（CSS 变量配色）；`check_html.py` / `check_links.py` 校验。

**源仓库（核实一切的权威）:** `/home/verden/course/hermes-agent/`（只读）。

---

## 全局规范（每个内容 task 都适用）

- **编辑对象**：只改 `src/part9.py`（内容）+ 必要的注册文件；ch6 补充图改 `src/part2.py`（ch6 在 part2）。
- **BUILD QUIRK — 每次 build 两遍**：`cd src && rm -rf __pycache__ && python3 build.py >/dev/null 2>&1; python3 build.py >/dev/null 2>&1 && python3 check_html.py 2>&1 | tail -1`。
- **诚实铁律**：所有 `file:line` 引用先跑 `sed -n` 比对、verbatim（HTML 转义 `<`→`&lt;` `>`→`&gt;` `&`→`&amp;`，Python 三引号写 `&quot;&quot;&quot;`）。配色只用 `var(--*)`（验证 `grep -c 'fill="#\|stroke="#' src/part9.py` = 0，除 `#fff` 图标）。SVG `role="img" aria-label`，font 主≥11/次要 9–10.5/emoji 22–26，元素不重叠不越界。zh/en 结构对称、en 无残留中文/U+3000/双 `——`。关键数字：429=1h、401=5m、unhealthy=10min、retry poll=200ms、压缩 0.50/85% 边缘。
- **风格参考**（先 view 再动手）：`src/part1.py` LESSON_04（沙漏）、`src/part2.py` LESSON_06（缓存对比）、`src/part8.py`（ch26 的 analogy/macro/深度块/SVG/要点卡/设计框模式）。
- **每个 task 末尾**：build 0 error + hex 0 + 提交。

---

## Task 1: 脚手架 — part9.py + 注册 + Part 8（先让空 ch27/28 能 build 通过）

**Files:**
- Create: `src/part9.py`
- Modify: `src/shell.py`（PAGES 加 ch27/28）
- Modify: `src/registry.py`（import part9 + CONTENT 2 条）
- Modify: `src/quizzes.py`（ch27/28 占位）

- [ ] **Step 1: 创建 `src/part9.py` 骨架**（zh/en 仅 lead 占位，后续 task 填）

```python
LESSON_27 = {
    "zh": r"""
<p class="lead">模型不是一个端点,而是一个需要<strong>调度</strong>的资源池——多把 API key 要轮换与熔断、不同的副任务要路由到不同模型、每个模型的上下文窗口要先<strong>算出来</strong>才能决定何时压缩。</p>
""",
    "en": r"""
<p class="lead">A model isn't an endpoint — it's a resource pool that needs <strong>scheduling</strong>: multiple API keys to rotate and trip, side tasks routed to different models, and each model's context window that must be <strong>computed</strong> before deciding when to compress.</p>
""",
}

LESSON_28 = {
    "zh": r"""
<p class="lead">一个跑了几十轮、还开着后台进程、正在改文件的 agent,要怎么<strong>安全地急停、可靠地知道后台干完没、原子地落盘</strong>?这一章拆开 Hermes 在运行时最容易出正确性 bug 的三套机制。</p>
""",
    "en": r"""
<p class="lead">An agent that has run dozens of turns, still has background processes open, and is mid-edit on a file — how does it <strong>stop safely, reliably know when background work finishes, and persist atomically</strong>? This chapter opens up the three runtime mechanisms most prone to correctness bugs.</p>
""",
}
```

- [ ] **Step 2: `src/shell.py` PAGES 注册**（在 ch26 条目后追加两行）

```python
    ("27-model-credentials-routing.html", "模型·凭证·路由层", "Model · credentials · routing",
     "第八部分 · 深入篇·模型与运行时内幕", "Part 8 · Deep Dive · Model & Runtime Internals"),
    ("28-runtime-resilience.html", "运行时韧性：中断·后台·文件", "Runtime resilience: interrupt · background · files",
     "第八部分 · 深入篇·模型与运行时内幕", "Part 8 · Deep Dive · Model & Runtime Internals"),
```

- [ ] **Step 3: `src/registry.py` 注册**（顶部加 import，CONTENT 里 ch26 行之后加 2 条）

```python
# 顶部 import 区：
import part9
# CONTENT dict 里：
    "27-model-credentials-routing.html": part9.LESSON_27,
    "28-runtime-resilience.html": part9.LESSON_28,
```

- [ ] **Step 4: `src/quizzes.py` 占位**（ch26 块后、最后 `}` 前插入；Task 10 替换为真题）

```python
    "27-model-credentials-routing.html": {"mcq": [], "open": []},
    "28-runtime-resilience.html": {"mcq": [], "open": []},
```

- [ ] **Step 5: build + 校验脚手架**

Run:
```bash
cd /home/verden/course/hermes-agent-visual-guide/src && python3 -c "import part9, shell, registry, quizzes; print('imports OK', '27-model-credentials-routing.html' in registry.CONTENT, '28-runtime-resilience.html' in registry.CONTENT)" && rm -rf __pycache__ && python3 build.py >/dev/null 2>&1; python3 build.py >/dev/null 2>&1 && python3 check_html.py 2>&1 | grep -E "27-|28-|error\(s\)" && python3 check_links.py 2>&1 | tail -1
```
Expected: `imports OK True True`；生成 `lessons/27-*.html` + `lessons/28-*.html`；check_html **0 error**（会有 CJK/visual-block/analogy/key-points WARN，正常，后续 task 消除）；links resolve（数量增至 ~106）。

- [ ] **Step 6: Commit**

```bash
cd /home/verden/course/hermes-agent-visual-guide && git add -A && git -c user.name="verdenmax" -c user.email="verdenmax@users.noreply.github.com" commit -m "feat(ch27-28): scaffold Part 8 deep-dive chapters + registration

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
```

<!-- Task 2-10 见下文追加 -->

---

## Task 2: ch27 开篇（lead + 🔌能源调度中心 + 🌍macro + 总览图 SVG）

**Files:** Modify `src/part9.py`（把 Task 1 的 `LESSON_27` lead 占位扩成完整开篇；en 同步）

- [ ] **Step 1: 写 zh 开篇**——保留 lead → 加 `<div class="card analogy">`（🔌 类比 · **能源调度中心**：一堆电厂=API key/provider，有的满载跳闸进冷却、有的彻底烧毁永久退役；调度员**绝不能反复去戳一座已确认没电的电厂**，又要为不同负载派不同机组，还得先知道每座的额定功率）→ `<div class="card macro">`（🌍 宏观 · 模型层三个隐藏复杂度：① 凭证是**有状态的池**不是一把 key；② "调用模型"不止主对话、还有一堆**副 LLM 任务**各走各的路由；③ "上下文多大"是**分层解析**出来的、直接决定压缩何时触发）→ 总览图 SVG（见 Step 3）。
- [ ] **Step 2: 写 en 开篇**——`LESSON_27["en"]` 对应英文，结构一致。
- [ ] **Step 3: 总览图 SVG（zh+en 各一份）**——`viewBox="0 0 680 H"`，画「模型层三套机制」全景：三栏（凭证池 / 副 LLM 路由 / context-length），每栏一句"它管什么 + 最大的坑"，底部一条共同主线「把模型当成需要调度的有状态资源，而非无状态端点」。配色全 `var(--*)`。`fig-cap`：「<b>模型层三套机制</b>：凭证要轮换熔断、副任务要路由、窗口要先算出来——三者都把'调用模型'从一次性动作变成有状态调度。」
- [ ] **Step 4: build + 校验**（命令见全局规范）。Expected: 0 error，ch27 analogy WARN 消除。
- [ ] **Step 5: Commit** `feat(ch27): opening (energy-dispatch analogy + macro) + overview SVG`

---

## Task 3: ch27 · 27.1 凭证池状态机（深度块 + 3 SVG）

**Files:** Modify `src/part9.py`（开篇后追加 27.1；en 同步）

**深度块模板（本两章统一，机制图为主、非 ❌/✅ 对比）：**
```html
<h3>🔬 27.1 · 凭证池：从一把 key 到有状态的资源池</h3>
<p>...机制说明 + 坑（含 <span class="mono">标识符</span>）...</p>
<div class="codefile"><div class="cf-head"><span class="dot"></span><span class="path">agent/credential_pool.py:250</span></div><pre>...verbatim...</pre></div>
<div class="figure"><svg ...>...</svg><div class="fig-cap">...</div></div>
```

- [ ] **Step 1: 27.1 机制说明 + verbatim 源码（zh）**——讲：同 provider 多 key 组持久化池，状态 `OK→EXHAUSTED(限时冷却)→DEAD(永久排除)`；坑=**绝不 re-probe 已确认空的 bucket**（DEAD 无条件排除、EXHAUSTED 只在 reset_at/TTL 后回归）；429→1h、401→5m，解析 provider reset_at 覆盖默认。verbatim 引 `_exhausted_ttl`。
  **核实命令**：`sed -n '250,256p' /home/verden/course/hermes-agent/agent/credential_pool.py`（`_exhausted_ttl`，401/429 分支）+ `grep -n "EXHAUSTED_TTL_429_SECONDS\s*=\|EXHAUSTED_TTL_401_SECONDS\s*=" /home/verden/course/hermes-agent/agent/credential_pool.py`（确认 429=3600/1h、401=300/5m 的真实常量值，正文用真实值）+ `sed -n '488,496p' .../credential_pool.py`（terminal auth failure → DEAD）。codefile path 标 `agent/credential_pool.py:250`。
- [ ] **Step 2: 3 张 SVG（zh）**——① **状态机图**：节点 OK / EXHAUSTED / DEAD，转移边标条件（429/401→EXHAUSTED+TTL；terminal-auth→DEAD；reset_at/TTL 到→回 OK；DEAD→reauth/sync）。② **限流冷却决策**：输入 HTTP 状态 → 401=5m / 429=1h / 其它=默认，reset_at 文本覆盖。③ **轮换策略对比**：一行 key 池（5 把），三种选法（random / least_used / round_robin）各高亮不同的下一把。每张带 fig-cap。
- [ ] **Step 3: en 同步**——27.1 机制说明 + codefile + 3 SVG 的英文版，结构一致。
- [ ] **Step 4: build + 校验 + 诚实抽查**——0 error；`grep -c 'fill="#\|stroke="#' src/part9.py` = 0；正文 429/401 冷却值与源码常量一致；DEAD/EXHAUSTED 语义与 :488/:250 一致。
- [ ] **Step 5: Commit** `feat(ch27): 27.1 credential-pool state machine + 3 SVGs`

<!-- Task 4-10 见下文追加 -->

---

## Task 4: ch27 · 27.2 副 LLM 路由（深度块 + 2 SVG）

**Files:** Modify `src/part9.py`（27.1 后追加 27.2；en 同步）

- [ ] **Step 1: 27.2 机制说明 + verbatim 源码（zh）**——讲：压缩/视觉/搜索/标题等**副任务**各自路由到最合适的 provider+model。`_resolve_auto()` 三段：① 用当前**主 provider+model**（运行期 base_url/key，由 `set_runtime_main()` 同步、不用陈旧 config）；② 查 `auxiliary.<task>.fallback_chain` 再顶层 fallback；③ discovery 链 `openrouter→nous→local/custom→api-key`。坑=per-task pinning 真实；**402/额度→标 provider unhealthy 藏 10 分钟**；通用 fallback 可能静默选更便宜的模型。
  **核实命令**：`grep -n "def _resolve_auto\|def _mark_provider_unhealthy\|def set_runtime_main" /home/verden/course/hermes-agent/agent/auxiliary_client.py`（确认 :3322 / :2395 / :1868）+ `sed -n '2395,2410p' .../auxiliary_client.py`（unhealthy 10min TTL，确认真实分钟数）+ `sed -n '3322,3340p' .../auxiliary_client.py`（_resolve_auto 开头三段结构）。codefile path 标 `agent/auxiliary_client.py:3322`（引 _resolve_auto 三段的关键骨架，可节选标「· 简化」）。
- [ ] **Step 2: 2 张 SVG（zh）**——④ **路由决策树**：从「副任务请求」入口 → ① 主 provider+model（命中？）→ ② fallback_chain/顶层 fallback → ③ discovery 链，途中**跳过 unhealthy 的 provider**（标红支路）。⑤ **per-task 路由矩阵**：表格化，行=任务（curator/vision/embedding/title/session_search），列=可被 `auxiliary.<task>.provider/.model` 独立钉，示意不同任务可指向不同模型。每张带 fig-cap。
- [ ] **Step 3: en 同步**——27.2 机制说明 + codefile + 2 SVG 英文版。
- [ ] **Step 4: build + 校验 + 诚实抽查**——0 error；hex 0；_resolve_auto/unhealthy/set_runtime_main 行号与核实命令一致；unhealthy 分钟数与源码一致。
- [ ] **Step 5: Commit** `feat(ch27): 27.2 auxiliary-LLM routing + 2 SVGs`

---

## Task 5: ch27 · 27.3 context-length→压缩阈值（深度块 + 2 SVG）+ ch27 收尾

**Files:** Modify `src/part9.py`（27.2 后追加 27.3 + ch27 收尾；en 同步）

- [ ] **Step 1: 27.3 机制说明 + verbatim 源码（zh）**——讲：压缩不是"到 X% 就压"，而是先**分层解析真实窗口**（config→持久缓存→各 provider probe→硬编码默认），再 `_compute_threshold_tokens()` 算阈值（小窗口/大 max_tokens 预留时用 85%、floor 在 MINIMUM_CONTEXT_LENGTH）。坑=某些 provider 要 bypass 陈旧缓存（LM Studio/Nous/Codex/Kimi/MiniMax/Grok）；窗口算错→压缩过早(浪费)或过晚(被拒)。
  **核实命令**：`grep -n "def get_model_context_length\|def _compute_threshold_tokens" /home/verden/course/hermes-agent/agent/model_metadata.py /home/verden/course/hermes-agent/agent/context_compressor.py`（确认 model_metadata.py:1613 附近 + context_compressor.py:742 附近）+ `sed -n '742,760p' .../context_compressor.py`（阈值计算）+ `grep -n "MINIMUM_CONTEXT_LENGTH\s*=\|0.85\|threshold" .../context_compressor.py | head`。codefile path 标 `agent/context_compressor.py:742`。
- [ ] **Step 2: 2 张 SVG（zh）**——⑥ **解析链瀑布图**：config → 持久缓存 → provider probe（Bedrock/自定义/本地/Anthropic/Codex/Nous/OpenRouter/models.dev）→ 硬编码默认，命中即返回（短路），标「某些 provider bypass 陈旧缓存」。⑦ **窗口→阈值图**：一条 token 数轴，标 resolved window、max_tokens 预留、触发阈值点（小窗用 85%），越过即触发压缩。每张带 fig-cap。
- [ ] **Step 3: ch27 收尾（zh）**——🎯 `<div class="card design">`（三套机制共享理念：**把模型当成需要调度的有状态资源，而非无状态端点**；呼应韧性/可演化 + 窄腰）+ 📌 `<div class="card key">`（📌 本课要点，5–6 条：凭证池状态机 + 不 re-probe 空 bucket；副 LLM 三段路由 + per-task pinning + unhealthy 缓存；context-length 分层解析驱动压缩阈值；诚实数字 429=1h/401=5m/unhealthy=10min/85% 边缘）。
- [ ] **Step 4: en 同步**——27.3 深度块 + 2 SVG + 设计框 + 要点卡（en `📌 Key points`）。
- [ ] **Step 5: build + 校验**——0 error；hex 0；**ch27 应已无 analogy/key-points/visual-block WARN**（内容+图齐）。
- [ ] **Step 6: Commit** `feat(ch27): 27.3 context-length->compression threshold + design card + key-points + 2 SVGs`

<!-- Task 6-10 见下文追加 -->

---

## Task 6: ch28 开篇（lead + 🔌飞行紧急程序 + 🌍macro）+ 28.1 中断传播扇出（深度块 + 2 SVG）

**Files:** Modify `src/part9.py`（`LESSON_28`；en 同步）

- [ ] **Step 1: ch28 开篇（zh）**——保留 lead → `<div class="card analogy">`（🔌 类比 · **飞行中的紧急程序**：急停不是瞬间的，飞行员拉杆后飞机要到**下一个稳定点**才改出=中断协作式；后台引擎要么真在转要么早停了、仪表要如实还不能重复报警=通知去重；改飞行计划先写草稿、**校验无误再原子换上**=temp+rename）→ `<div class="card macro">`（🌍 宏观 · 运行时三个正确性陷阱：① 中断**只在检查点生效**且**不会自动传给子代理**；② 后台"持久"几乎全是**进程内幻觉**；③ 落盘原子性**挂在一个不起眼的 rename 上**）。
- [ ] **Step 2: 28.1 机制说明 + verbatim 源码（zh）**——讲：`/stop` 设 `agent._interrupt_requested`，主循环**在循环边界轮询**；子代理传播需**显式** `child.interrupt()`；流式/retry 也各有检查（retry sleep 每 200ms poll）。坑=**协作式非抢占**（API/tool 中途不立即停，到下个检查点才停）；**子代理传播极易漏**（父必须显式传下去）。
  **核实命令**：`grep -n "_interrupt_requested" /home/verden/course/hermes-agent/agent/conversation_loop.py | head`（确认 :594 循环边界检查、:1397 retry）+ `grep -n "\.interrupt(\|_interrupt_requested\|interrupt_fn" /home/verden/course/hermes-agent/tools/delegate_tool.py | head`（确认 child 传播 :1658 附近）。codefile path 标 `agent/conversation_loop.py:594`（引 while 边界的中断检查）。
- [ ] **Step 3: 2 张 SVG（zh）**——⑧ **中断扇出图**：中心 `_interrupt_requested=True` → 四条传播路径（主循环边界 / 子代理需显式 child.interrupt() / 流式回调 / retry 200ms poll），子代理那条标「易漏：必须显式传播」。⑨ **cooperative vs preemptive 时序**：一条时间轴，interrupt 在某 tool 调用**中途**到达 → 标「挂起等待」→ 到下个循环边界才真正停（对比抢占式的瞬停虚线）。每张带 fig-cap。
- [ ] **Step 4: en 同步**——ch28 开篇 + 28.1 深度块 + 2 SVG 英文版。
- [ ] **Step 5: build + 校验**——0 error；hex 0；ch28 analogy WARN 消除；中断行号与核实一致。
- [ ] **Step 6: Commit** `feat(ch28): opening (flight-emergency analogy) + 28.1 interrupt propagation + 2 SVGs`

---

## Task 7: ch28 · 28.2 后台进程生命周期（深度块 + 2 SVG）

**Files:** Modify `src/part9.py`（28.1 后追加 28.2；en 同步）

- [ ] **Step 1: 28.2 机制说明 + verbatim 源码（zh）**——讲：后台进程登记在内存 registry（`_running`/`_finished`），带输出缓冲、watch 通知、wait/poll/kill；崩溃恢复靠 `processes.json` 检查点（**仅为网关崩溃**）。`notify_on_complete` 在 `_move_to_finished()` 发一次完成通知。坑=**durability 几乎全进程内**（重启即丢，detached 恢复只认 host-PID，是尽力而为）；要跨重启用 cron 或 `terminal(background, notify_on_complete)`（呼应 ch26 E6）；**通知去重**靠 was_running 守卫 + 消费端 dedupe；watch 命中限流（`WATCH_STRIKE_LIMIT`）会禁用 watch、翻转 notify_on_complete。
  **核实命令**：`grep -n "def _move_to_finished\|WATCH_STRIKE_LIMIT\s*=\|notify_on_complete" /home/verden/course/hermes-agent/tools/process_registry.py | head`（确认 _move_to_finished@1022、WATCH_STRIKE_LIMIT@69）+ `sed -n '1022,1040p' .../process_registry.py`（_move_to_finished + dedupe 注释 :1037）。codefile path 标 `tools/process_registry.py:1022`。
- [ ] **Step 2: 2 张 SVG（zh）**——⑩ **生命周期 + 通知去重图**：running →（watch 命中限流? WATCH_STRIKE_LIMIT=3 → 禁 watch、翻转 notify）→ `_move_to_finished` → notify，经**两道闸**（was_running 守卫 + 消费端 dedupe 集合）才发一次。⑪ **durability 对比**：三栏——进程内 background（重启=全丢）/ detached（host-PID 尽力恢复）/ cron（真跨重启持久）。每张带 fig-cap。
- [ ] **Step 3: en 同步**——28.2 深度块 + 2 SVG 英文版。
- [ ] **Step 4: build + 校验 + 诚实抽查**——0 error；hex 0；_move_to_finished/WATCH_STRIKE_LIMIT 行号与核实一致。
- [ ] **Step 5: Commit** `feat(ch28): 28.2 background-process lifecycle + notify dedupe + 2 SVGs`

---

## Task 8: ch28 · 28.3 安全文件编辑管线（深度块 + 2 SVG）+ ch28 收尾

**Files:** Modify `src/part9.py`（28.2 后追加 28.3 + ch28 收尾；en 同步）

- [ ] **Step 1: 28.3 机制说明 + verbatim 源码（zh）**——讲：read/write/patch/search 工具，写与 patch 走 `_atomic_write()`——在**目标同目录**建 temp（mktemp）、写、`mv -f` 原子换上；patch 后**再读回**验证字节落盘；保 BOM/CRLF；只报新增 lint 错。坑=**原子性挂在"同目录 temp+rename"**（跨文件系统 rename 非原子，temp 位置 load-bearing）；`patch_replace()` 要求模糊匹配**唯一**、写后 re-read 校验、**并发编辑会让校验失败**；路径安全 deny-list、大文件分页、二进制/图片拦截、search 行级。
  **核实命令**：`grep -n "def _atomic_write\|def patch_replace" /home/verden/course/hermes-agent/tools/file_operations.py`（确认 _atomic_write@937、主 patch_replace@1465）+ `sed -n '937,960p' .../file_operations.py`（temp+mv -f）。codefile path 标 `tools/file_operations.py:937`（引 _atomic_write 的 temp+rename）。
- [ ] **Step 2: 2 张 SVG（zh）**——⑫ **安全编辑管线**：目标同目录建 temp → 写 → 读回验证字节 →（失败回滚）→ `mv -f` 原子换上。⑬ **跨 FS rename 破原子性**：同 FS rename=原子（一步指针切换）vs 跨 FS=copy+unlink（中途崩溃留半个文件）——所以 temp 必须和目标同目录。每张带 fig-cap。
- [ ] **Step 3: ch28 收尾（zh）**——🎯 `<div class="card design">`（三套机制守的都是**异步世界里的正确性边界**：中断协作契约、后台诚实状态、落盘原子保证；是「自主连续运行」可靠性的地基）+ 📌 `<div class="card key">`（5–6 条：中断协作式非抢占 + 子代理需显式传播；后台 durability 是进程内幻觉 + 跨重启用 cron；通知去重双闸；原子写靠同目录 temp+rename + 写后校验；并发编辑会校验失败）。
- [ ] **Step 4: en 同步**——28.3 深度块 + 2 SVG + 设计框 + 要点卡。
- [ ] **Step 5: build + 校验**——0 error；hex 0；ch28 无 WARN。
- [ ] **Step 6: Commit** `feat(ch28): 28.3 atomic file-edit pipeline + design card + key-points + 2 SVGs`

<!-- Task 9-10 见下文追加 -->

---

## Task 9: #7 — ch6 缓存补充图（独立小任务）

**Files:** Modify `src/part2.py`（ch6 = `LESSON_06`，加 1 张 SVG；en 同步）

- [ ] **Step 1: 在 ch6 合适处加 1 张 SVG（zh）**——主题「Anthropic 缓存 **4 断点放置 + thinking 签名安全**」：`system + 末 3 条非 system` 打 4 个 `cache_control` 断点（同 TTL，注入前 deepcopy）；**cache_control 必须从 thinking/redacted_thinking 块移除**否则破坏签名验证；带 marker 的 system prompt 保持 content blocks 而非纯字符串。`<div class="figure">` + fig-cap。**不改 ch6 既有文字**（除非加一句过渡引出图）。
  **核实命令**：`sed -n '49,79p' /home/verden/course/hermes-agent/agent/prompt_caching.py`（4 断点 + deepcopy + TTL）+ `grep -n "thinking\|redacted_thinking\|cache_control" /home/verden/course/hermes-agent/agent/anthropic_adapter.py | sed -n '1,12p'`（确认 :1793-1812 附近移除 cache_control 的逻辑）。
- [ ] **Step 2: en 同步**——ch6 en 加对应 SVG。
- [ ] **Step 3: build + 校验**——0 error；hex 0；ch6 zh/en SVG 对称。
- [ ] **Step 4: Commit** `feat(ch6): add Anthropic cache 4-breakpoint + thinking-signature SVG`

---

## Task 10: ch27/28 quiz + 最终全量验证 + 打印版 + README/index + push

**Files:** Modify `src/quizzes.py`（替换 ch27/28 占位）+ `README.md` + （build 产物）

- [ ] **Step 1: 写 ch27 + ch28 quiz**（替换 Task 1 的占位；先 view ch26 quiz 块确认 `q/opts/answer/why` + `open` 双语结构，严格照填，**无裸双引号/裸 `<`**）：
  - **ch27 mcq1**：「为什么凭证池绝不该去重试一个刚被确认为空（DEAD/已知耗尽）的 key？」正解：会浪费往返、还可能被进一步惩罚——DEAD 无条件排除、EXHAUSTED 只在 reset_at/TTL 后才回归。
  - **ch27 mcq2**：「副 LLM 路由 `_resolve_auto` 的第一优先级是什么？」正解：当前会话的**主 provider+model**（运行期同步，非陈旧 config）。
  - **ch27 open**：举一个你会为副任务（如压缩/标题）单独钉模型的场景，说明它对应哪条设计线。
  - **ch28 mcq1**：「为什么一个 tool 调用进行到一半时 `/stop` 不会立刻停？」正解：中断是**协作式非抢占**，只在循环边界检查。
  - **ch28 mcq2**：「后台进程在进程重启后消失的根因？」正解：durability 几乎全是**进程内**状态，要跨重启得用 cron / 持久化。
  - **ch28 open**：举一个后台任务你会用 cron 而非 `background=true` 的场景，说明原因。
  - 写完 `cd src && python3 -c "import quizzes; print('27', len(quizzes.QUIZZES['27-model-credentials-routing.html']['mcq']), '28', len(quizzes.QUIZZES['28-runtime-resilience.html']['mcq']))"` 确认非空 + `_validate()` 过。
- [ ] **Step 2: README + index 章数**——`README.md`：`26 chapters`→`28 chapters`（en + zh `26 章`→`28 章`），「What it covers」表加一行 `| 8 | Deep dive — model/credentials/routing & runtime resilience | 27–28 |`。index 由 build 自动含新章（Step 3 验证）。
- [ ] **Step 3: 最终全量验证**

Run:
```bash
cd /home/verden/course/hermes-agent-visual-guide/src && rm -rf __pycache__ && python3 build.py >/dev/null 2>&1; python3 build.py >/dev/null 2>&1 && python3 check_html.py 2>&1 | tail -2 && python3 check_links.py 2>&1 | tail -1 && echo "ch27 CJK:" $(python3 -c "import re,part9; print(len(re.findall(r'[\u4e00-\u9fff]',part9.LESSON_27['zh'])))") "ch28 CJK:" $(python3 -c "import re,part9; print(len(re.findall(r'[\u4e00-\u9fff]',part9.LESSON_28['zh'])))") && echo "part9 SVG:" $(grep -c '<svg' part9.py) && echo "hex:" $(grep -c 'fill=\"#\|stroke=\"#' part9.py) && echo "index 28 ch:" $(grep -oE 'lessons/[0-9]+-' ../index.html | sort -u | wc -l)
```
Expected: check_html **0 error**（ch27/28 无 WARN）；links 全解析；两章 CJK 各 ≥2500；part9 SVG ≥14（每章 7+）；hex **0**；index 含 28 章。

- [ ] **Step 4: 诚实抽查（全部 🔬 锚点）**——逐条跑 Task 3-9 的「核实命令」，确认 part9.py 的 codefile 与源码一致；特别确认：429=1h/401=5m、unhealthy=10min、retry=200ms、_move_to_finished@1022、_atomic_write@937、_resolve_auto@3322。`grep -n $'\u3000' src/part9.py` 仅可在 zh 出现、`grep -c '——' `（en 段不得有双 em-dash）。
- [ ] **Step 5: 打印版**——`cd src && python3 build_print.py 2>&1 | tail -1`，Expected `Wrote 2 print files (28 lessons each)`。
- [ ] **Step 6: Commit + push**

```bash
cd /home/verden/course/hermes-agent-visual-guide && git add -A && git -c user.name="verdenmax" -c user.email="verdenmax@users.noreply.github.com" commit -m "feat(ch27-28): quizzes + final verification + print + README (28-chapter guide)

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>" && git push origin master
```
（push 触发 GitHub Pages 自动重新部署。）

---

## Self-Review（plan 写完自查）

- **Spec coverage**：spec 的 6 深度块 → Task 3(27.1)/4(27.2)/5(27.3)/6(28.1)/7(28.2)/8(28.3) 全覆盖；#7 → Task 9；2 章开篇 → Task 2/6；收尾(设计框+要点卡) → Task 5/8；quiz → Task 10；脚手架 → Task 1；SVG 总数 spec ~13 → 总览(T2)+3(T3)+2(T4)+2(T5)+2(T6)+2(T7)+2(T8)=14 + ch6 补充(T9) ✓；README/index/print → Task 10 ✓。
- **诚实铁律**：每个 🔬 task 带「核实命令」+ Task 10 Step 4 统一抽查；关键数字（429=1h/401=5m/unhealthy=10min/200ms/1022/937/3322）显式校验 ✓。锚点已在 spec self-review 抽查（修正过 process_registry 一处）。
- **配色一致**：全局规范 + 每 SVG task 的 hex 抽查 ✓。
- **无 placeholder**：每个 task 给了 analogy/机制要点/SVG 画什么/codefile 锚点 + 核实命令 + commit；深度块模板在 Task 3 给全；quiz 题目在 Task 10 给全 ✓。
- **执行顺序**：Task 1（脚手架）→ 2-5（ch27）→ 6-8（ch28）→ 9（ch6 图）→ 10（quiz+验证+部署），每个走 implementer → spec 审查 → 质量审查 双审循环。




